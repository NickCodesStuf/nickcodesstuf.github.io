filters:
  and:
    - file.ext == "md"
views:
  - type: bases-cms
    name: Posts
    filters:
      and:
        - file.folder.startsWith("posts")
    imageProperty: note.image
    titleProperty: note.title
    showDraftStatus: true
    draftStatusProperty: note.draft
    newNoteLocation: /posts
    cardSize: 250
    imageAspectRatio: 0.95
    dateProperty: note.pubDate
    descriptionProperty: note.tags
